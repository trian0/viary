package com.trian0.viary.ui.theme

import androidx.compose.ui.graphics.Color

// Cores Primárias (Baseadas no Azul Ciano do Viary)
val ViaryPrimary = Color(0xFF00BFFF)     // Ciano Vibrante
val ViaryOnPrimary = Color(0xFFFFFFFF)   // Texto sobre Ciano (Branco)
val ViaryPrimaryContainer = Color(0xFFE0F7FA) // Container de Primária (Ciano Claro)
val ViaryOnPrimaryContainer = Color(0xFF001F25) // Texto sobre Container (Quase Preto)

// Cores Secundárias/Acentuação (Baseadas no Laranja/Vermelho e Roxo)
val ViarySecondary = Color(0xFFFF6347)   // Laranja Avermelhado (Accent)
val ViaryOnSecondary = Color(0xFFFFFFFF) // Texto sobre Laranja (Branco)
val ViarySecondaryContainer = Color(0xFFFFEBEE) // Um tom claro de Laranja para containers
val ViaryOnSecondaryContainer = Color(0xFF410000) // Texto sobre Laranja Claro

// Cores de Destaque (Terciárias)
val ViaryTertiary = Color(0xFF8A2BE2)     // Roxo/Violeta
val ViaryOnTertiary = Color(0xFFFFFFFF)
val ViaryTertiaryContainer = Color(0xFFEADDFF)
val ViaryOnTertiaryContainer = Color(0xFF21005D)

// Cores de Erro (Padrão, mas mantendo a estrutura)
val ViaryError = Color(0xFFBA1A1A)
val ViaryOnError = Color(0xFFFFFFFF)
val ViaryErrorContainer = Color(0xFFFFDAD6)
val ViaryOnErrorContainer = Color(0xFF410002)

// Cores Neutras (Fundo e Superfície)
val ViaryBackground = Color(0xFFFFFFFF)    // Fundo Principal (Branco)
val ViaryOnBackground = Color(0xFF212121)  // Texto Principal (Quase Preto)
val ViarySurface = Color(0xFFFFFFFF)       // Superfície (Cards, Elevados)
val ViaryOnSurface = Color(0xFF212121)     // Texto sobre Superfície
val ViarySurfaceVariant = Color(0xFFF5F5F5) // Variação de Superfície (Cinza Claro)
val ViaryOnSurfaceVariant = Color(0xFF757575) // Texto/Ícones Secundários

// Cor do Contorno/Borda
val ViaryOutline = Color(0xFFCCCCCC)